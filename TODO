change limit / offset to long

check if bug with smart quotes is fixed

add subscriptions:

  - For organizations that I watch, email me whenever a service is created

  - For organizations that I watch, email me whenever a new service version is uploaded

  - For services that I watch, email me whenver a new version is uploaded

Org level setting to enable semver versioning (default on)
  - prevent backwards incompatible changes once at major version 1

Support for testing

Replace ValidationError with com.gilt.apidoc.models.Error

Email user when their membership request has been processed

Implement subscriptions.finaAll authorization filter